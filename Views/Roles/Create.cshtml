@model Mmt.Beheer.MVC.ViewModels.Role.RolePostViewModel

@{
    ViewData["Title"] = "Create";
}

<h1 class="text-info text-center">Create new Role</h1>

<h4 class="text-primary">Create new Role</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
    <div>
        <p>
            attention! this page only helps you to create a new role in the database, you stil need to apply the authorization in the controllers
        </p>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
